密码学 习题集 - 认证加密与高级加密构造
第 1 题
攻击者截获了如下密文（十六进制编码）：
20814804c1767293b99f1d9cab3bc3e7 ac1e37bfb15599e5f40eef805488281d
攻击者知道明文是字符串 "Pay Bob 100 $ " 的 ASCII 编码。攻击者还知道所使用的加密方式是以 AES 为底层分组密码、带随机 IV 的 CBC 加密。
请说明攻击者可以如何修改密文，使其解密后变为 "Pay Bob 500 $ "。修改后的密文是什么（十六进制编码）？
修改后的密文：
20814804C1767293BD9F1D9cab3BC3E7AC1E37Bfb15599E5F40eEF805488281d
第 2 题
设 (E, D) 是一个加密系统，提供认证加密。以下哪些系统也提供认证加密？（可能有多个正确答案）
[×]  E'(k, m) = (E(k, m), 0)，且解密时若 b=0 返回 D(k,c) 否则返回 ⊥
[ ] E'(k, m) = (E(k, m), 0)，且解密时直接返回 D(k, c)
[×]  E'(k, m) = E(k, m) ⊕ 1^s，且 D'(k, c) = D(k, c ⊕ 1^s)
[]  E'(k, m) = (E(k, m), E(k, m))，且解密时若 D(k, c1) = D(k, c2) 返回 D(k, c1) 否则返回 ⊥
第 3 题
如果需要在同一个密钥下加密多条消息，应该使用哪种加密方式？
[ ] 自行实现 MAC-then-Encrypt
[ ] 使用标准实现的随机化计数器模式
[ ] 使用标准实现的带随机 IV 的 CBC 加密
[×] 使用标准实现的认证加密模式之一，例如 GCM、CCM、EAX 或 OCB
第 4 题
定义 MAC 系统：S(k, m) := E(k, m)，V(k, m, t) := 1 若 D(k, t) = m 否则 0。为了使该 MAC 系统安全，加密系统 (E, D) 需要满足什么性质？
[×] 认证加密
[ ] 确定性选择明文攻击下的语义安全
[ ] 完美保密性
[ ] 语义安全
第 5 题
假设 k 是从密钥空间 {0, 1}^256 中采样得到的非均匀秘密密钥（最高有效 128 位全为 0）。设 F(k, x) 是安全 PRF。以下哪个构造在密钥均匀采样时是安全 PRF，但在该非均匀分布采样时是不安全的？
[ ] F'(k, x) = F(k, x) 若 MSB_128(k) = 0^128；否则为 1^256
[×] F'(k, x) = F(k, x) 若 MSB_128(k) ≠ 0^128；否则为 1^256
[ ] F'(k, x) = F(k, x)
[ ] F'(k, x) = F(k, x) 若 MSB_128(k) ≠ 1^128；否则为 0^256
第 6 题
在什么场景下可以使用像 SIV 这样的确定性认证加密（DAE）？
[] 在同一个密钥下反复加密同一条固定消息
[ ] 在语音通话中使用同一个密钥分别加密许多数据包
[ ×] 消息从足够大的空间中随机选取，因此消息不太可能重复
[ ] 在同一个密钥下加密数据库中的多条记录，并且相同记录可能重复多次
第 7 题
设 E(k, x) 是一个安全分组密码。考虑可调分组密码：E'((k1, k2), t, x) = E(k1, x) ⊕ E(k2, t)。这个可调分组密码是否安全？
[ ] 不安全，因为对于 t ≠ t'，有特定异或等式成立
[ ×] 不安全，因为对于 x ≠ x' 且 t ≠ t'，有特定异或等式成立
[] 不安全，因为对于 x ≠ x'，有 E'((k1,k2),0,x) ⊕ E'((k1,k2),1,x) = E'((k1,k2),0,x') ⊕ E'((k1,k2),1,x')
[ ] 不安全，因为对于 x ≠ x'，有 E'((k1,k2),0,x) ⊕ E'((k1,k2),0,x) = E'((k1,k2),0,x') ⊕ E'((k1,k2),0,x')
[ ] 是安全的，前提是 E 是安全分组密码
第 8 题
假设我们尝试只使用第二步，由 AES 构造一个定义域为 10^16 的保留格式 PRP。每次求值期望需要多少次 AES 迭代？
[×] 2^128 / 10^16 ≈ 3.4 × 10^22
[ ] 2^16
[ ] 10^16
[ ] 10^16 / 2^128
第 9 题
定义 MAC 系统：S(k, m) := E(k, m, 0)，V(k, m, tag) := 1 若 E(k, m, 0) = tag 否则 0。这个 MAC 安全吗？
[ ] 这取决于具体的可调分组密码
[× ] 安全
[] 不安全
第 10 题
攻击者截获了一段 64 字节密文（16字节 IV + 48字节载荷）。为了在最坏情况下解密完整的 48 字节载荷，填充预言机攻击需要多少次选择密文查询？
[× ] 12288
[ ] 1024
[] 12240
[ ] 256
第 11 题
考虑可调分组密码构造：E'(k, (t, i), x) = E_PRP(k, x ⊕ P(t, i)) ⊕ P(t, i)。这是否是一个安全的可调 PRP？
[ ] 是的，它是安全的
[] 不安全：E(k, (t,1), P(t,2)) ⊕ E(k, (t,2), P(t,1)) = P(t,1)
[ ×] 不安全：E(k, (t,1), P(t,1)) ⊕ E(k, (t,2), P(t,2)) = P(t,1) ⊕ P(t,2)
[ ] 不安全：E(k, (t,1), P(t,1)) ⊕ E(k, (t,2), P(t,2)) = 0
第 12 题
使用固定 IV 的计数器模式是否是 CPA 安全的？
[ ] 是（Yes）
[×] 否（No）
[ ] 视情况而定（Depends）
第 13 题
在密钥派生函数（KDF）中，CTX（上下文参数）的目的是什么？
[×] 即使两个应用采样相同的 SK，它们也会获得独立的密钥
[ ] 给字符串标注应用程序名称是良好的做法
[ ] 这没有意义
第 14 题
你会如何重新设计 SSH 以抵抗非原子解密和长度字段攻击？
[× ] 发送未加密但经过 MAC 认证的长度字段
[×] 用 encrypt-then-MAC 替换 encrypt-and-MAC
[ ×] 在长度字段后立即添加对 (seq-num, length) 的 MAC
[ ] 移除长度字段，通过每次接收一个字节后验证 MAC 来识别数据包边界
第 15 题
如果 TLS 使用计数器模式（CTR）而不是 CBC 模式，填充预言机攻击是否仍然有效？
[ ] 是的，填充预言机影响所有加密方案
[ ] 取决于使用哪种分组密码
[×] 不，计数器模式不需要使用填充
第 16 题
在 CBC 模式中，攻击者希望修改 IV，使得解密后的第一块明文从 "dest=80..." 变为 "dest=25..."。攻击者应该将 IV 修改为什么值 IV'？
[ ] IV' = IV ⊕ (... 25 ...)
[ ] IV' = IV ⊕ (... 80 ...)
[×] IV' = IV ⊕ (... 80 ...) ⊕ (... 25 ...)
[ ] 无法实现（It cannot be done）